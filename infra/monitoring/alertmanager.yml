global:
  resolve_timeout: 5m

route:
  receiver: "team-webhook"
  group_by: ["alertname", "job", "severity"]
  group_wait: 15s
  group_interval: 2m
  repeat_interval: 4h

receivers:
  - name: "team-webhook"
    webhook_configs:
      - url: "${ALERT_WEBHOOK_URL}"
        send_resolved: true
